# Protocol Definitions

This directory owns `.proto` source files and generated-code integration points.

Generated files must be placed under the build tree, not committed into the source tree unless a later decision explicitly changes this rule.
