# CMake Layout

This directory contains reusable CMake modules, toolchain files, and build helpers.

Phase 0 keeps this directory as a boundary only. Dependency discovery and compiler checks are introduced in the implementation phases defined by `docs/tasks.md`.
